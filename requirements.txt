# 아래 버전(이상)으로 전체 파이프라인(분석A/B/Q, train.py, predict.py, tests/)
# 검증 완료. np.trapz가 sklearn.metrics.auc로 대체되는 등 numpy/scikit-learn
# 메이저 업데이트로 호환성이 깨진 적이 실제로 있었으므로, 최소 버전을 명시한다.
pandas>=2.0
numpy>=2.0
scikit-learn>=1.5
xgboost>=2.0
ruptures>=1.1        # PELT 변화점 탐지 (분석A 1단계 비교용 보조)
lifelines>=0.27      # Kaplan-Meier (1단계 비교)
shap>=0.45           # 모델 해석
joblib>=1.3          # 모델 저장/로드
streamlit>=1.30      # webapp
plotly>=5.20         # webapp 인터랙티브 차트 (세그먼트 클릭 선택 등)
pytest>=8.0          # tests/ 실행용
